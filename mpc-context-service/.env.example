# Port for the MPC Context Service
MPC_SERVICE_PORT=3001
PORT=3001 # Fallback if MPC_SERVICE_PORT is not set by config

# Example Database connection (if you choose to use a DB)
# DB_HOST=localhost
# DB_USER=youruser
# DB_PASSWORD=yourpassword
# DB_NAME=mpc_context_db

# Example Existing API details (if MPC service needs to call it)
# EXISTING_API_URL=http://localhost:8080/api
# EXISTING_API_KEY=your_other_api_key
